# directions/

Optional human-written research direction files.

Keep these as plain Markdown or text. The harness includes them in the OpenCode context through `AGENTS.md`/config and the iteration prompt.

Examples:

```text
directions/ideas.md
directions/constraints.md
directions/ablation-plan.md
```
